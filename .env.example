# ── OpenClaw Dashboard Environment Variables ──────────────────────────

# JWT signing secret (required if auth is enabled)
# Used only for signing/verifying JWT tokens — NOT as a login password.
JWT_SECRET=change-me-to-a-random-secret

# Login password (recommended: bcrypt hash, e.g. output of `npx bcryptjs-cli hash "mypassword"`)
# If omitted, falls back to DASHBOARD_SECRET for legacy compat.
DASHBOARD_PASSWORD=$2b$10$...your-bcrypt-hash-here...

# Legacy: used as both JWT secret AND password (deprecated — use JWT_SECRET + DASHBOARD_PASSWORD instead)
# DASHBOARD_SECRET=

# PostgreSQL connection string (optional — enables persistent sessions, tasks, etc.)
DATABASE_URL=postgresql://user:password@localhost:5432/openclaw_dashboard

# OpenClaw Gateway WebSocket URL (required for gateway data source)
GATEWAY_WS_URL=ws://localhost:4444

# Gateway auth token (if the gateway requires one)
# GATEWAY_TOKEN=

# Server
PORT=3000
HOSTNAME=localhost
NODE_ENV=development
